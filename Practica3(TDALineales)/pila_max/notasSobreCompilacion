####Detalles sobre la compilación.

ERROR DE COMPILACIÓN (Preguntado a Raquel)

`g++ src/pruebaLista.cpp lib/libpractica3.a -Iinclude/ -o pruebaLista`

Cuando intentamos compilar haciendo uso de una librería creada que contiene los objetos que hemos creado o conjuntos de funciones
necesitamos indicarle donde está la carpeta que incluye las cabeceras de esos datos. ¿Por qué? Si quisiéramos pasarle a alguien
la librería no tendríamos por qué pasarle los ficheros de cabecera, tan sólo el fichero .a que representa la empaquetación de todos
los objetos que nosotros hayamos incluido en esa librería.

El proceso de creación de los objetos de la librería es:

  g++ -c -o obj/Vector.o src/Vector.cpp -I include/

  con -c no generamos el código ejecutable sino el código objeto en el archivo que le especificamos con -o, si no especificamos el nombre
  con -o lo creará con el mismo nombre que el fichero .cpp. Además incluimos la carpeta donde están las cabeceras.

  g++ -c -o obj/Lista.o src/Lista.cpp -I include/


Una vez creado los objetos los añadimos a una librería:

  ar rvs lib/libprueba.a obj/Lista.o obj/Vector.o
  ar: creando lib/libprueba.a
  a - obj/Lista.o
  a - obj/Vector.o


Al intentar compilar con una librería da un error:

g++ -o pruebaVector src/pruebaVector.cpp -L lib/libprueba.a
src/pruebaVector.cpp:2:20: fatal error: Vector.h: No existe el archivo o el directorio
#include <Vector.h>
                   ^
compilation terminated.

Pero si funciona como lo tenemos en el makefile, pasado primero a codigo objeto, por lo que a efectos del proyecto,
funciona.
Aún no se ha explicado.
